// SELECT characters.species, characters.char_name FROM characters JOIN status ON characters.id = status.character_id WHERE char_name = 'Rick Sanchez';

// SELECT characters.species, characters.char_name
// FROM characters
// JOIN location
// ON characters.id = location.id
// WHERE origin = 'unknown';
